{
  "name": "capyvivi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && cp -r public dist",
    "dev": "wrangler dev --assets ./public",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.12.0"
  }
}
